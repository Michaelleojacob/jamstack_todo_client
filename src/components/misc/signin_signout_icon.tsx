import LockOutlinedIcon from "@mui/icons-material/LockOutlined";
import { Box } from "@mui/material";
const Lock = () => {
  return (
    <div>
      <Box className="lock_background">
        <LockOutlinedIcon fontSize="large" />
      </Box>
    </div>
  );
};

export default Lock;
