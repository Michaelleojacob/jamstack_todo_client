import { Button } from "@mui/material";
import MenuIcon from "@mui/icons-material/Menu";

const FakeBurger = () => {
  return (
    <div style={{ visibility: "hidden" }}>
      <div>
        <MenuIcon />
      </div>
    </div>
  );
};

export default FakeBurger;
